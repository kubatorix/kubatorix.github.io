# Dependencies
node_modules/

# Lockfile (project uses npm + package-lock.json)
yarn.lock
pnpm-lock.yaml

# Playwright / E2E artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/
screenshots/

# Archives & local exports
*.zip
*.tar
*.tar.gz
*.tgz

# Design references (not used in build)
секции/

# Environment & secrets
.env
.env.*
!.env.example

# OS & editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Logs & caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/
.parcel-cache/
.turbo/

# AI / local tooling
.claude/

# Optional: keep .vscode in repo (settings.json is tracked)
